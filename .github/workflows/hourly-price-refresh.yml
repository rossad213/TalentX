name: Legacy combined hourly refresh (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Category workflows now own market refreshes
        run: |
          echo "The combined hourly workflow is intentionally disabled."
          echo "Sports market refresh owns game pricing."
          echo "Music market refresh owns Music events and outcomes."
          echo "Actor market refresh owns Actor events and outcomes."
          echo "Creator market refresh owns Creator state."
          echo "Publish unified TalentX market recombines category states and deploys GitHub Pages."
