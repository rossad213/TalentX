TALENTX — CURRENT DEPLOYMENT NOTES

The repository is already connected to Vercel. Normal approved changes committed to main deploy automatically.

CURRENT CATALOG
- The deployed beta reads data/current_catalog.json.
- That file currently contains 200 curated prototype profiles.
- Do not describe those records as automated or live-verified.

EXPERIMENTAL LARGE-CATALOG WORKFLOW
- The workflow named "Build current catalog baseline" can run weekly or manually.
- Its 10,000-record value is a validation target for candidate output, not the live site count.
- Successful output is uploaded as a temporary Actions artifact.
- It does not automatically replace the Vercel catalog.

SAFE PUBLICATION
Before replacing the live catalog, review source coverage, licensing, identity matches, categories, roles, teams/platforms, career statuses, verification timestamps, confidence values, duplicate detection, and browser performance.

All prices, scores, charts, movements, portfolios, and trades are simulated.
